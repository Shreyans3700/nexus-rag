# CPU-only torch build — sentence-transformers (reranker) pulls torch
# transitively, and the default PyPI wheel bundles the full CUDA stack
# (multi-GB) even though the reranker here only ever runs on CPU.
--extra-index-url https://download.pytorch.org/whl/cpu
torch

fastapi==0.139.0
langchain==1.3.13
langchain-core==1.4.9
langchain-community==0.4.2
langchain-text-splitters==1.1.2
python-dotenv==1.2.2
asyncpg==0.31.0
langchain-openai==1.3.4
slowapi>=0.1.9
redis>=4.2.0
uvicorn==0.51.0
tiktoken>=0.7.0
# RAG dependencies
pymilvus==2.5.8
pypdf==5.1.0
python-docx==1.1.2
python-multipart==0.0.20
numpy>=1.26.0
sentence-transformers==5.5.0
# Queue and storage
arq>=0.26
minio>=7.2.0
